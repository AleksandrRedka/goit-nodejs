let Joi;_2fe‍.x([["contactValidateMiddleware",()=>contactValidateMiddleware]]);_2fe‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);

const contactValidator = Joi.object({
  name: Joi.string().required(),
  email: Joi.string().required(),
  phone: Joi.string().required()
})

       const contactValidateMiddleware = (req, res, next) => {
  const { error } = contactValidator.validate(req.body)
  if (error) {
    res.status(400).send({ message: 'Missing required name field' })
    return
  }
  next()
}
